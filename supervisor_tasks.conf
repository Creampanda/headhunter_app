; supervisor config file

; [unix_http_server]
; file=/var/run/supervisor.sock   ; (the path to the socket file)
; chmod=0700                       ; sockef file mode (default 0700)

[inet_http_server]
port = 0.0.0.0:9001  ; runs in docker, so global is okay

[supervisord]
logfile=./supervisord.log ; (main log file;default $CWD/supervisord.log)
pidfile=./supervisord.pid ; (supervisord pidfile;default supervisord.pid)
childlogdir=./log/supervisor            ; ('AUTO' child log dir, default $TEMP)

; the below section must remain in the config file for RPC
; (supervisorctl/web interface) to work, additional interfaces may be
; added by defining them in separate rpcinterface: sections
[rpcinterface:supervisor]
supervisor.rpcinterface_factory = supervisor.rpcinterface:make_main_rpcinterface

[supervisorctl]
serverurl=unix:///var/run/supervisor.sock ; use a unix:// URL  for a unix socket

[program:refresh_data_1]
autostart=True
stopsignal=QUIT
user=hints
directory=/home/hints/app
command=python3 refresh_data.py

[program:refresh_data_2]
autostart=True
stopsignal=QUIT
user=hints
directory=/home/hints/app
command=python3 refresh_data.py

[program:refresh_data_3]
autostart=True
stopsignal=QUIT
user=hints
directory=/home/hints/app
command=python3 refresh_data.py